{
  "name": "portfolio-cms",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site'",
    "seed": "node lib/seed.mjs",
    "deploy": "netlify deploy --prod",
    "logs": "netlify logs:function api"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
